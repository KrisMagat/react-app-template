// import React, { useState } from 'react';
// import { ProgressPlugin } from 'webpack';

// function Example() {

//   //fetch function to set initial state
//   let state;
//     fetch(api).then(data => state = data)

//   //new variable with updated state      //pass in new state to this fn                //initial state
//   const [user,                                  setUser] =                         useState(state);

//   //fetch call to add a user
//   const addUser = () => {
//     fetch(api).then((data) => (setUser(data)));
//   };

//   return (
//     <div>
//       <p>You clicked {user} times</p>
//       <Component name = ${props.name}  />
//       <button onClick={() => addUser}>Click me</button>
//     </div>
//   );
// }
